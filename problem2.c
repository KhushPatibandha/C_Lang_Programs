#include <stdio.h>

int main()

{
   int radius = 3;
   int height = 3;
   float pi = 3.14;
   
   printf("Area of circle is %f\n", pi * radius * radius);

   printf("volume of a cylinder is %f\n", pi * radius * radius * height);



}