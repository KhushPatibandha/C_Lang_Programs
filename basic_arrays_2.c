#include<stdio.h>
int main()
{
    // int a[] = {10, 20, 30};

    // printf("The value of a[0] is %i\n", a[0]);
    // printf("The value of a[1] is %i\n", a[1]);
    // print f("The value of a[2] is %i\n", a[2]); 


    // float a[] = {10.1, 20.2, 30.3};

    // printf("The value of a[0] is %.1f\n", a[0]);
    // printf("The value of a[1] is %.1f\n", a[1]);
    // printf("The value of a[2] is %.1f\n", a[2]); 


}